alias <SHORTCUT> "connect <IP> ; password <PASSWORD>" //Used to quickconnect to servers

//Soda Popper -- Shorthand Alias for enabling the auto-reload for soda popper. Just type sp in console
alias sp "sp_enable"
alias sp_enable "alias sp sp_disable; +reload"
alias sp_disable "alias sp sp_enable; -reload"
